#include<stdio.h>
int ismode()
{
    int i,n,maxelement,maxcount;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    maxelement=a[0];  
    maxcount=1;
    for(i=0;i<n;i++)
    {
        if()
    }
}